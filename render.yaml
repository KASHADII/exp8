services:
  - type: web
    name: flask-rest-api
    env: python
    plan: free
    rootDir: backend/rest-api-lab
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
